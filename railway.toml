[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "dotnet AgendaFlow.Api.dll"
healthcheckPath = "/health/live"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
